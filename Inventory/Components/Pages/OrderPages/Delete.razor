@page "/orders/delete"
@using Inventory.Application
@using Inventory.Domain
@using Microsoft.AspNetCore.Authorization
@inject NavigationManager NavigationManager
@inject IOrderService Service

@attribute [Authorize]

<PageTitle>Delete</PageTitle>

<h1>Delete</h1>

<p>Are you sure you want to delete this?</p>
<div>
    <h2>Order</h2>
    <hr />
    @if (order is null)
    {
        <p><em>Loading...</em></p>
    }
    else {
        <dl class="row">
            <dt class="col-sm-2">DateTime</dt>
            <dd class="col-sm-10">@order.DateTime</dd>
        </dl>
        <dl class="row">
            <dt class="col-sm-2">Number</dt>
            <dd class="col-sm-10">@order.Number</dd>
        </dl>
        <dl class="row">
            <dt class="col-sm-2">AuthorId</dt>
            <dd class="col-sm-10">@order.AuthorId</dd>
        </dl>
        <dl class="row">
            <dt class="col-sm-2">IsAssigned</dt>
            <dd class="col-sm-10">@order.IsAssigned</dd>
        </dl>
        <dl class="row">
            <dt class="col-sm-2">AssigneeFromId</dt>
            <dd class="col-sm-10">@order.AssigneeFromId</dd>
        </dl>
        <dl class="row">
            <dt class="col-sm-2">AssigneeToId</dt>
            <dd class="col-sm-10">@order.AssigneeToId</dd>
        </dl>
        <dl class="row">
            <dt class="col-sm-2">LocationFromId</dt>
            <dd class="col-sm-10">@order.LocationFromId</dd>
        </dl>
        <dl class="row">
            <dt class="col-sm-2">LocationToId</dt>
            <dd class="col-sm-10">@order.LocationToId</dd>
        </dl>
        <EditForm method="post" Model="order" OnValidSubmit="DeleteOrder" FormName="delete" Enhance>
            <button type="submit" class="btn btn-danger" disabled="@(order is null)">Delete</button> |
            <a href="/orders">Back to List</a>
        </EditForm>
    }
</div>

@code {
    private Order? order;

    [SupplyParameterFromQuery]
    private string? Id { get; set; }

    protected override async Task OnInitializedAsync()
    {
        if (Id is not null)
            order = await Service.Get(Id);

        if (order is null)
            NavigationManager.NavigateTo("notfound");
    }

    private async Task DeleteOrder()
    {
        await Service.Delete(order!);

        NavigationManager.NavigateTo("/orders");
    }
}
